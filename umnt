#!/bin/bash

function main() {
	if [ "$1" == "" ]; then
		umount ${HOME}/mnt/.h
		umount ${HOME}/mnt/media
	elif [ "$1" == "bdi" ]; then
		umount ${HOME}/mnt/bdi
	elif [ "$1" == "nlp" ]; then
		umount ${HOME}/mnt/nlp
	elif [ "$1" == "gpu2" ]; then
		umount ${HOME}/mnt/gpu2
	fi
}

main $1
